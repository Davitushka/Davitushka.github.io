# assets

Drop screenshots here to fill the project cards (16:9 works best):

- `thread-sync.png` — thread-sync SOC dashboard
- `protonoro-timer.png` — Protonoro Timer app

Optional:

- `bg.mp4` — custom animated background video (auto-used if present, otherwise particles show)
- `favicon.png`, `og.png`

Until a file exists, the card shows a styled placeholder automatically.
